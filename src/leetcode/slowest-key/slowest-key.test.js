import { slowestKey } from './slowest-key';
describe('[Leetcode] slowest-key', () => {
  test('example #1', () => {
    expect(slowestKey()).toBe('');
  });

  test('example #2', () => {
    expect(slowestKey()).toBe('');
  });
});
